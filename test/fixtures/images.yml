<% require 'file_path_utils' %>

<% 1.upto(50) do |i| %>
    image_<%= i %>:
        user: quentin
        title: Image (<%= i %>)
        description: a image file
        file_path: <%= file_path_import(:model => 'image', :id => Fixtures.identify("image_#{i}"), :file_path => 'test.png') %>
<% end %>