<%= display_objects_list(
    #:collection => @paginated_objects,
    :in_list_partial => 'people/people_list',
    :ajax_url => people_path,
    #:ajax_url => request.path+'?'+request.url.split('?').last,
    :no_div => no_div
) %>