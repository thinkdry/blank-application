<div class="pubmed_item">
  <h2><%= item.title %></h2>
  <div class="description"><%= sanitize item.description, :tags => %w(p) %></div>
  <div class="links">
    <%= image_tag("rss_add.png") %>
  	<%= link_to 'Importer dans Artic', new_publication_path(:pubmed_item_id => item.id) %>
  	&nbsp;-&nbsp;
  	<%= image_tag("link.png") %>
  	<%= link_to 'Lien vers Pubmed', item.link, :popup=>['new_window'] %>
    <%# Specify the weight & width :popup=>['new_window','height=800,width=1024'] %>
  </div>
</div>