<% item_reactive_content_tag(:li, object) do %>	
	<%= ('(' + object.class.to_s + ')').ljust(13).gsub(' ', '&nbsp;') %>
	<b><%= object.title %></b>
	par
	<%= object.user.firstname.capitalize %>
	<%= object.user.lastname.capitalize %>
<% end %>