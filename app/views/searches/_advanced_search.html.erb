    <% if category == 'item' || available_items_list.include?(category) %>
		<div><%= render :partial => 'items/advanced_search', :locals => { :category => category } %></div>
    <% end %>
	<div class="dateSelection">
	      <label><%=I18n.t('general.common_word.from1')%>: </label>
		  <div class="formElement">
		  	<%= calendar_date_select_tag 'search[created_at_after]', @search.created_at_after, :class => "input_with_calendar" %>
		  </div>
	      <label><%=I18n.t('general.common_word.to1')%>: </label>
		  <div class="formElement">
		  	<%= calendar_date_select_tag 'search[created_at_before]', @search.created_at_before, :class => "input_with_calendar" %>
		  </div>
	</div>
