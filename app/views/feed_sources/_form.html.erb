<% form_for @current_object, :builder => ArticFormBuilder do |f| %>
  <%= f.text_field :name %>
  <%= f.text_field :url %>
  <% f.field(:submit, :label => '') do  %>
    <%= submit_tag 'Ajouter' %>
  <% end %>
<% end %>