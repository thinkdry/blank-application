<fieldset class="myform">
  <legend>General settings</legend>

  <% form_for :general, :url => general_changing_superadministration_path, :html => {:multipart => true} do |f| -%>

      Logo management :<br />
      <%= image_tag(url_for_file_column(@logo, 'picture_path', 'thumb')) if (@logo && @logo.picture_path) %><br />
      <%= file_column_field 'general', 'picture_path' %><br />
      <br />
      
      Available items :<br />
      <%= checkboxes_from_list(ITEMS, "items", @conf) %>
      
      Available languages :<br />
      <%= checkboxes_from_list(LANGUAGES, "languages", @conf) %>

      Workspaces types :<br />
      <%= checkboxes_from_list(WS_TYPES, "ws_types", @conf) %>

      Feed items imporation types :<br />
      <%= checkboxes_from_list(FEED_ITEMS_IMPORTATION_TYPES, "feed_items_importation_types", @conf) %>
      
      <br />

      <div class="buttons" id="area_BUTTONS" style="padding-top: 2em">
      <button type="submit" class="positive"><img src="/images/tick.png" alt=""/>Mettre à jour</button>
      <button type="reset" class="negative"><img src="/images/cross.png" alt=""/>Annuler</button>
     </div>
<% end %>





</fieldset>
