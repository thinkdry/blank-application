<div>
  Available permissions :<br />
  <% for permission in Permission.all - role.permissions %>
    <%= check_box_tag(role.name+"[available]"+"[#{permission.name}]", 1, checked=false) %>
    <%= permission.name %>
  <% end %>
</div>

<br />

<div>
  Current permissions :<br />
  <% for permission in role.permissions %>
    <%= check_box_tag(role.name+"[current]"+"[#{permission.name}]", 1, checked=true) %>
    <%= permission.name %>
  <% end %>
</div>