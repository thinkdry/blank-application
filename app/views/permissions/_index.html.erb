<h1>Listing Permissions</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
  </tr>
<%# @permissions=Permission.find(:all)  %>
<% for permission in @permissions %>
  <tr>
      <td><%=h permission.name %></td>
      <td><%=h permission.description %></td>
      <td><%#= link_to 'Show', permission_path(permission) %></td>
      <td><%= link_to_remote 'Edit', :update => "permissions", :url => { :action => "edit", :id => permission.id}, :method => :get %></td>
      <td><%= link_to_remote 'Delete', :update => "permissions", :url => { :action => "destroy", :id => permission.id}, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to_remote "New Permission",
    :update => "permissions",
    :url => {:action => "new"} %>
