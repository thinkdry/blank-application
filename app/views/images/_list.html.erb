<div id="content_bis">
	<div id="area_CONTENUS">
		<div class="titre"><%= image_tag Image.icon %><strong>Images</strong></div>
			<% @current_objects.each do |co| %>
				<%= item_in_list co %>
			<% end %>
			<%= link_to "Ajouter un ficher", new_item_path(Image), :class => "add_item"%>
		</div>
	</div>
</div>
