<% item_show( :title => 'Image',  :object => @current_object ) do %>
	<%= link_to(image_tag(@current_object.image.url.split("?")[0], :class => "image_peview"), @current_object.image.url.split("?")[0])%>
<% end %>
