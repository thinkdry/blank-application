<fieldset>
  <legend><%= @current_object.new_record? ? I18n.t('comment.new.title') : I18n.t('comment.edit.title') %></legend>
  <%# form_for @current_object, :url => (@current_object.new_record? ? comments_path : comment_path(@current_object.id)), :builder => BlankFormBuilder, :html => {:method => (@current_object.new_record? ? :post : :get)} do |f| %>
  <% form_for @current_object, :builder => BlankFormBuilder do |f| %>

    <%= @current_object.commentable_type+' : '+@current_object.commentable.title %><br />
    <%= f.text_area :text, :label => I18n.t('comment.field.text') %>
    <%#= f.text_field :email, :label => I18n.t('comment.field.text') %>
    <%#= f.text_field :nickname, :label => I18n.t('comment.field.text') %>

    <%= f.select :state, ['posted', 'validated', 'rejected'], :label => I18n.t('comment.field.state') %>

    <%= f.submit(@current_object.new_record? ? I18n.t('general.button.create') : I18n.t('general.button.update')) %>

  <% end %>

  
</fieldset>