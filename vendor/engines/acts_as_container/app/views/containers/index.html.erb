<div class="list_header">
	<div class="homePartLogo">
		<img src="/images/icons/<%= params[:controller].split('/')[1].capitalize.singularize.downcase%>.gif">
	</div>
	<div class="homePartList">
		<h1 class="<%= params[:controller].split('/')[1].capitalize.singularize.downcase%>"><%= params[:controller].capitalize.singularize %></h1>
	    <p><%= image_tag("/images/icons/add.png")%>Ajouter un <%= link_to t("container.#{params[:controller].split('/')[1].capitalize.singularize.downcase}") ,new_container_path(params[:controller].split('/')[1].singularize) %></p>
	</div>
</div>

<%= render :partial => 'containers/index' %>
